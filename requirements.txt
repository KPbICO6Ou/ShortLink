# Production dependencies.
# Keep in sync with [project.dependencies] in pyproject.toml.
# pyproject.toml is the source of truth; this file is provided for
# convenience (`pip install -r requirements.txt`).

fastapi>=0.110
uvicorn[standard]>=0.27
sqlmodel>=0.0.22
pydantic-settings>=2.2
typer>=0.12
rich>=13.7
httpx>=0.27
qrcode[pil]>=7.4
